### 以 Camera 遮盖的范围作为主界面，参照 Scene  Title => Main Canvas
    1. Canvas -> Pamel -> Image : Source Image
    2. 在 Canvas 中的 Render Camera 拖入 Main Camera
    3. Canvas Scaler 中通过设定 UI Scale Model(Scale With Screen Size) 
    4. 通过原题的比例，设置Reference Resolution 的高和宽
    5. Screen Match Mode(Match Width or Height)

### 常用
    1. Canvas -> Pamel -> Image : Source Image
    2. Canvas Scaler 中通过设定 UI Scale Model(Scale With Screen Size)
    3. 通过原题的比例，设置Reference Resolution 的高和宽
    4. Screen Match Mode(Match Width or Height)

### layoutgroup


+ **子物体和父物体完全一样大小**，shift+alt+ Anchor Presets 右下角